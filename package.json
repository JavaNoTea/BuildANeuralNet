{
  "name": "neural-network-builder",
  "version": "1.0.0",
  "description": "Visual drag-and-drop neural network designer with PyTorch code generation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build"
  },
  "keywords": ["neural-network", "pytorch", "machine-learning", "visual-editor", "react"],
  "author": "JavaNoTea",
  "license": "GPL-3.0",
  "packageManager": "pnpm@10.11.0",
  "devDependencies": {
    "@types/react": "^19.1.6",
    "@types/react-dom": "^19.1.5",
    "turbo": "^2.5.3"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  }
}
